%h1 Listing Continuous Integration Mappings

%table.zebra-striped
  %tr
    %th CI Value
    %th Test Suite
    %th

  - if @ci_mappings.nil? or @ci_mappings.count == 0
    %tr
      %td{:colspan => "3"}
        %h4 No data
  - else
    - @ci_mappings.each do |ci_mapping|
      %tr
        %td= ci_mapping.ci_value
        %td= ci_mapping.test_suite.name
        = render :partial => 'partial/table_actions', :locals => {:show_url => project_ci_mapping_path(@project, ci_mapping), :edit_url => edit_project_ci_mapping_path(@project, ci_mapping)}

= link_to 'New Ci mapping', new_project_ci_mapping_path, :class => 'btn primary'
