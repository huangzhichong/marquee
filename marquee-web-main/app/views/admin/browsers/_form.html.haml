-if flash[:error]
  .alert-message.error= flash[:error].html_safe

= form_for([:admin, @browser]) do |f|
  -if @browser.errors.any?
    #error_explanation
      %h2= "#{pluralize(@ci_mapping.errors.count, "error")} prohibited this browser from being saved:"
      %ul
        - @browser.errors.full_messages.each do |msg|
          %li= msg
  %fieldset
    %legend= title
    .clearfix
      = f.label :name
      .input
        = f.text_field :name
    .clearfix
      = f.label :version
      .input
        = f.text_field :version
    .actions
      = f.submit 'Save', :class => 'btn primary'
      = link_to 'Back', admin_browsers_path, :class => 'btn'